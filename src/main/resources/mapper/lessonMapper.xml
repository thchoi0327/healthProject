<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.health.mapper.LessonMapper">

	<!-- 작성후 DB에 올림 -->
	<insert id="insertBoardwrite">
		 <![CDATA[
		     insert into lessonBoard values(
		     #{name},#{gender},#{goal},#{age},#{day},#{wishtime},
		     #{tgender},#{wish},#{place},#{memo},sysdate
		   	 )
		 ]]>
	</insert>



</mapper>	 
